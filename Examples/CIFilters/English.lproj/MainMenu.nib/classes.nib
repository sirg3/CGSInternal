{
    IBClasses = (
        {
            ACTIONS = {apply = id; }; 
            CLASS = Controller; 
            LANGUAGE = ObjC; 
            OUTLETS = {nameField = NSTextField; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}